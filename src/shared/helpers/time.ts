export const convertSecondsToMs = (seconds: number) => seconds * 1000

export const convertMinutesToMs = (minute: number) => minute * 60000

export const convertHoursToMs = (hours: number) => hours * 3600000

export const convertDaysToMs = (days: number) => days * 86400000
