import styled from "styled-components"

export const Tag = styled.span`
	background: ${({ theme }) => theme.ui.color.secondary_50}46;
	border-radius: 20px;
	padding: 4px 8px;
	display: inline-block;
`
