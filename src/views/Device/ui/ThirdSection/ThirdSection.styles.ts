import styled from "styled-components"

export const $ThirdSection = styled.section`
	margin-top: 50px;
	display: flex;
	flex-direction: column;
	align-items: center;

	& > .third-section {
		&__title {
			text-align: center;
		}

		&__add-comment {
			margin-top: 40px;
		}
	}
`
