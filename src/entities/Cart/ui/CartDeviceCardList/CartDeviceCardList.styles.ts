import styled from "styled-components"

export const $CartDeviceCardList = styled.div`
	display: flex;
	flex-direction: column;
	gap: 30px;

	.device-cart-list {
		&__favorite {
			margin-bottom: -5px;
			margin-right: 10px;
		}
	}
`
